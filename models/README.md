# 模型

将下载的检查点放在这里。不要提交它们，除非用户指定提交某个模型且许可证、体积和保密边界允许。每条运行记录都必须标明精确模型或检查点版本。

## 动态登记

首次确认具体本地模型或检查点时，由 Codex 创建 `models/registry.yaml`；没有实际模型时不创建空登记表或虚假示例。每个条目至少记录 `id`、`name`、`source`、`version`、`license` 和 `path`；如上游使用 revision、commit、checksum 或精确权重标识，一并记录。不得登记访问令牌或受限下载地址。
