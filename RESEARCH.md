# 研究任务合同

本文件只保存当前方向、必要的阶段一活动状态、最终研究设计、持久决策、动作触发授权、证据摘要和工作流投影。High 方向专用窗口不修改本文件；切换 Medium 后才一次性写入有实质变化的交接摘要。详细检索证据、实验任务和写作任务分别进入 `research/`、`experiments/TODO.md` 和 `paper/TODO.md`。

## 当前工作流状态

- 当前阶段：阶段一：调研与设计。
- 阶段一子状态：INTAKE。
- 阶段状态：未开始。
- 阶段门禁：待检查。
- 停止原因：不适用。
- 恢复条件：不适用。
- 最近确认日期：尚无。

阶段切换只通过 `scripts/transition_workflow.py` 完成。

## 阶段一活动区

- 最近轮次：尚未开始。
- 当前全局阶段：阶段一：调研与设计。
- 当前阶段一子状态：INTAKE。
- 当前工作方向：尚未形成。
- 替代方向检查：待进行。
- 关键未知：用户尚未提供研究种子。
- 下一步：询问研究意图、动机和硬边界。

High 的 1–3 轮只在对话中保留临时方向和方法级讨论。切换 Medium 后，由 Medium 仅在内容发生实质变化时一次性更新本节、方向形成字段和一条合并交互记录；其余章节仅在真实对象出现或到达检查点时新增内容。

### 方向形成字段

| 方向字段 | 当前摘要 | 信息来源 | 状态 | 最近轮次 | 未决问题 ID |
| --- | --- | --- | --- | --- | --- |
| 用户研究意图 | 待形成 | 尚无 | 待澄清 | 尚无 | 待分配 |
| 研究对象 | 待形成 | 尚无 | 待澄清 | 尚无 | 待分配 |
| 核心问题 | 待形成 | 尚无 | 待澄清 | 尚无 | 待分配 |
| 创新性假设 | 待形成 | 尚无 | 待澄清 | 尚无 | 待分配 |
| 最小可证伪路径 | 待形成 | 尚无 | 待澄清 | 尚无 | 待分配 |
| 范围边界 | 待形成 | 尚无 | 待澄清 | 尚无 | 待分配 |

状态只使用 `已澄清`、`暂定`、`待澄清`、`明确未知`。`明确未知` 必须引用 `OPEN-<nnn>`。

### 精简交互记录

| 轮次 | 问题焦点 | 用户信号 | 方向变化 | 关键未知 | 下一步 |
| --- | --- | --- | --- | --- | --- |

High 交接中的非正式 IDEA 和方法级讨论由 Medium 仅在影响方向判断时压缩到交互记录，不单独创建文件。达到正式验证条件时，才创建 `DIR-<nnn>`。

### 正式入围方向

| 候选方向 ID | 核心问题 | 创新性假设 | 最小验证路径 | 主要风险 | 状态 | 选择、合并或否决理由 | 决策 ID |
| --- | --- | --- | --- | --- | --- | --- | --- |

允许只有一个正式方向。标记为 `已选定` 前必须向用户展示拟定方向、核心问题、范围边界和方法概览并取得明确确认；进入 `DRAFT` 前必须恰好一个方向为 `已选定`。其他真实方向使用 `已否决` 或 `已合并` 并说明理由。

### 关键未决问题

| 未决问题 ID | 问题 | 状态 | 影响 | 解决条件 |
| --- | --- | --- | --- | --- |

## 最终方向

尚未形成。方向选择检查点在本节写入已选 `DIR`、方向摘要、实质差异、最小可证伪路径、主要风险、范围边界、剩余未知以及选择依据；决策和证据只引用稳定 ID。

## 研究设计

方向确认后再创建真实的 `RQ`、`CONTRIB`、`SC`、`DATA`、`BASE`、`METRIC` 和 `exp` 记录。详细对象表只保存定义与关系；确认依据集中在“设计确认状态”。

### 设计确认状态

| 合同字段 | 当前值或引用 | 信息来源 | 确认状态 | 最近确认日期或轮次 | 决策 ID | 证据 ID |
| --- | --- | --- | --- | --- | --- | --- |
| 最终研究方向 | 待形成 | 尚无 | 待确认 | 尚无 | 待分配 | 待分配 |
| 数据、baseline、指标与成功标准 | 待设计 | 尚无 | 待确认 | 尚无 | 待分配 | 待分配 |
| 阶段二实验与执行方案 | 待设计 | 尚无 | 待确认 | 尚无 | 待分配 | 待分配 |
| 数据、许可与隐私边界 | 待设计 | 尚无 | 待确认 | 尚无 | 待分配 | 待分配 |
| 范围边界 | 待形成 | 尚无 | 待确认 | 尚无 | 待分配 | 待分配 |

### 研究对象与实验关系

尚未形成。方向确认后按 `docs/PHASE_ONE_PROTOCOL.md` 创建真实对象和 `RQ → DATA → BASE → METRIC → SC → exp` 关系，不保留空对象表。

- 必要消融与稳健性验证：待设计。
- 负面结果的有效完成条件：待设计。
- 阶段二失败停止规则：待设计。

### 数据与执行边界

- 数据分类、许可与公开限制：待设计。
- 个人信息、内部或受限属性：待设计。
- 外部传输：待设计；计划外发时创建限定范围的 `AUTH-<nnn>`。
- 计算与外部服务：待设计；只登记实际使用的 GPU、API、模型和数据流。
- 依赖与 Skills：待设计；只登记实际需要的变更或授权动作。

## 动作触发授权

仅在付费、外发、受限访问、依赖变更或其他高风险动作即将发生时新增记录。

| 授权 ID | 授权事项 | 范围 | 用户确认日期或轮次 | 状态 | 关联决策 ID |
| --- | --- | --- | --- | --- | --- |

## 调研证据摘要

只记录影响方向或研究设计的最小摘要；详细内容保存在 `research/`。

| 主张 ID | 摘要 | 来源 ID | 支持对象 | 核验状态 |
| --- | --- | --- | --- | --- |

## 当前决策

只有最终方向、正式方向处置、数据选择、核心指标、重要方法取舍和范围变化等实质决策使用 `DEC-<nnn>`。

| 决策 ID | 日期 | 决策 | 理由摘要 | 影响对象 | 用户确认状态 | 证据 ID |
| --- | --- | --- | --- | --- | --- | --- |

## 阶段切换记录

| 切换 ID | 日期 | 原阶段 | 新阶段 | 门禁结果 | 原因或授权 | 决策 ID |
| --- | --- | --- | --- | --- | --- | --- |

记录必须连续；最新真实 `TRANS` 的新状态必须与顶部状态一致。回退新增记录，不修改历史。
